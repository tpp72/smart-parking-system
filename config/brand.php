<?php

/*
|--------------------------------------------------------------------------
| แบรนด์ของระบบ (ชื่อ + โลโก้)
|--------------------------------------------------------------------------
| ค่าเริ่มต้นคือตรา "P" ที่วาดด้วย HTML — ไม่ต้องมีไฟล์ภาพ
| ถ้ามีโลโก้จริง วางไฟล์ไว้ใน public/ แล้วตั้ง BRAND_LOGO เป็น path จาก public เช่น images/brand-logo.svg
| ใช้ที่เดียวกันทั้งแถบเมนู หน้าสาธารณะ และหัวอีเมล (อีเมลต้องเป็น URL เต็ม ระบบจะเติม APP_URL ให้เอง)
*/

return [

    'name' => 'Smart Parking',

    'logo' => env('BRAND_LOGO'),

    // ความสูงของโลโก้บนหน้าจอ (px) — กว้างปรับตามสัดส่วนภาพ
    'logo_height' => (int) env('BRAND_LOGO_HEIGHT', 32),

];
